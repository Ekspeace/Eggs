<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ekspeace.eggs">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Eggs">
        <activity android:name=".Activity.RecipeDetails"/>
        <activity android:name=".Activity.CheckOut" />
        <activity android:name=".Activity.AddToBasket" />
        <activity android:name=".Activity.ResetPassword" />
        <activity android:name=".Activity.Main" />
        <activity android:name=".Activity.Login" android:noHistory="true" />
        <activity android:name=".Activity.StartUp" />
        <activity android:name=".Activity.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>